# Security policy

DEOS is pre-alpha and is not yet suitable for safety-critical or security-critical deployment.

Security-sensitive design goals include signed update/package metadata, explicit destructive plans, protected secrets, optional secure boot/flash encryption, and denying unrestricted hardware/root access to AI providers.

Do not store production credentials in example manifests or commit them to the repository.
